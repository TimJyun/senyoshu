pub const APP_ICON_256_256: manganis::ImageAsset =
    manganis::mg!(image("./assets/icon-256.png").format(ImageType::Webp));

pub const FORWARD_12_12: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-forward-50.png")
        .size(12, 12)
        .format(ImageType::Webp));

pub const LEFT_TOP_NAVIGATION_ITEM_HEIGHT: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-left-50.png")
        // = TOP_NAVIGATION_ITEM_HEIGHT
        .size(24, 24)
        .format(ImageType::Webp));

pub const MORE_MENU: manganis::ImageAsset = manganis::mg!(image("./assets/icons8-more-50.png")
    // = TOP_NAVIGATION_ITEM_HEIGHT
    .size(24, 24)
    .format(ImageType::Webp));

pub const BOOK_BOTTOM_NAVIGATION_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-book-50.png")
        // = BOTTOM_NAVIGATION_ITEM_HEIGHT
        .size(32, 32)
        .format(ImageType::Webp));

pub const SHELF_BOTTOM_NAVIGATION_ITEM_HEIGHT: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-shelf-62.png")
        // = BOTTOM_NAVIGATION_ITEM_HEIGHT
        .size(32, 32)
        .format(ImageType::Webp));

pub const CUSTOMER_BOTTOM_NAVIGATION_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-customer-64.png")
        // = BOTTOM_NAVIGATION_ITEM_HEIGHT
        .size(32, 32)
        .format(ImageType::Webp));

pub const REFRESH_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-refresh-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const LOGOUT_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-logout-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));
pub const RELOAD_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-reload-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const SAVE_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-save-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

// == AUDIO_IMG but size is not equal
pub const AUDIO_MARK_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-audio-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const MUTE_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-mute-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const QUESTION_MARK_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-question-mark-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const CLEAN_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-clean-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const RESET_MENU_ITEM: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-reset-50.png")
        // = MENU_ITEM_SIZE
        .size(12, 12)
        .format(ImageType::Webp));

pub const SETTING_BUTTON: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-setting-50.png")
        .size(24, 24)
        .format(ImageType::Webp));

pub const SWITCH_ON: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-switch-50-on.png")
        .size(24, 24)
        .format(ImageType::Webp));

pub const SWITCH_OFF: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-switch-50-off.png")
        .size(24, 24)
        .format(ImageType::Webp));

pub const SNOWFLAKE_IMG: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-snowflake-50.png")
        .size(16, 16)
        .format(ImageType::Webp));

pub const FIND_IMG: manganis::ImageAsset = manganis::mg!(image("./assets/icons8-find-50.png")
    .size(20, 20)
    .format(ImageType::Webp));

pub const AUDIO_IMG: manganis::ImageAsset =
    manganis::mg!(image("./assets/icons8-audio-50.png").format(ImageType::Webp));

pub const SYNC: manganis::ImageAsset = manganis::mg!(image("./assets/icons8-sync-50.png")
    .size(24, 24)
    .format(ImageType::Webp));
